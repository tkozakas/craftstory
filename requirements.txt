# Python dependencies for RVC voice conversion
# Install in a venv with Python 3.10:
#   uv venv .venv --python 3.10
#   source .venv/bin/activate
#   uv pip install -r requirements.txt

edge-tts>=6.1.0
rvc-python>=0.2.0
setuptools>=70.0.0

# Pin torch/torchaudio to versions compatible with rvc-python
# PyTorch 2.6+ changed weights_only default which breaks hubert model loading
torch>=2.0.0,<2.6
torchaudio>=2.0.0,<2.6
